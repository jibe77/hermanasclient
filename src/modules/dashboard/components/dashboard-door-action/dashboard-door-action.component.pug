li.nav-item.dropdown.dropdown-user.no-caret(
    ngbDropdown,
    placement='bottom-right',
    display='dynamic'
)
    a#userDropdown.nav-link(
        ngbDropdownToggle,
        data-cy='userMenu',
        role='button',
        aria-haspopup='true',
        aria-expanded='false'
    )
        fa-icon([icon]='["fas", "ellipsis-v"]')
    .dropdown-menu(
        ngbDropdownMenu,
        aria-labelledby='dropdownUser'
    )
        a(*ngIf='doorStatus !== undefined && doorStatus !== "OPENED"').dropdown-item((click)="openDoor()" )
            | Open door
        a(*ngIf='doorStatus !== undefined && doorStatus !== "CLOSED"').dropdown-item((click)="closeDoor()" )
            | Close door
